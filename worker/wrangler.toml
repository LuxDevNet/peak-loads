name = "peak-loads-r2-worker"
main = "worker.js"
compatibility_date = "2024-09-01"

# Bind your R2 bucket. In Cloudflare Dashboard, create an R2 bucket named "sdge-vault" (or your preferred name)
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "sdge-vault"
